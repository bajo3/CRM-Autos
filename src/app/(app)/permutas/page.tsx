import { ModuloPlaceholder } from "@/components/modulo-placeholder";

export default function Page() {
  return (
    <ModuloPlaceholder
      titulo="Permutas / Toma de usados"
      descripcion="Usados que entrega el cliente, con valor pretendido vs. tasado."
      etapa="Etapa 6 — Ventas y operaciones"
      proximaAccion="CRUD de permutas vinculadas a la venta y al tasador interno."
      tablaLista="permuta"
    />
  );
}
